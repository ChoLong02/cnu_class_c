#include <stdio.h>

// 1.주석
//   + 전처리 단계에서 모두 제거 됨
//   + 종류(/**/ or //)

// 2.문장 끝(;)
//   + 세미콜론(;)을 사용하여 문자 끝을 표시
//   + 한 줄에 한 문장씩 작성(가독성)
//   + 들여쓰기 명시하기(Level 확인)

// 3.함수()
//   + 반복적인 작업을 위해 작성한 코드묶음
//   + ()가 붙어 있음
//   + ()는 입력, ()안에 void를 적으면 입력 없음 의미
//   + return은 출력, return을 만나면 함수 종료
//   + 

// 4.printf() and puts
//   + 출력 함수 
//   + ()안에 출력하고 싶은 

int main(void) {
    
    return 0;
}